name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g wrangler@4
      - name: Deploy
        run: wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      - name: Set worker secrets
        run: |
          if [ -n "$EXPORT_KEY" ]; then
            echo "$EXPORT_KEY" | wrangler secret put EXPORT_KEY
          else
            echo "EXPORT_KEY repo secret not set; skipping"
          fi
          if [ -n "$RESEND_API_KEY" ]; then
            echo "$RESEND_API_KEY" | wrangler secret put RESEND_API_KEY
          else
            echo "RESEND_API_KEY repo secret not set; skipping"
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          EXPORT_KEY: ${{ secrets.EXPORT_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
